@model IEnumerable<Data_Training.Models.Enrolment>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Enrolment</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Lesson.Name)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Lesson.DateTime)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
<tr>
    <td>
        @Html.DisplayFor(modelItem => item.Lesson.Name)
    </td>
    <td>
        @Html.DisplayFor(modelItem => item.Lesson.DateTime)
    </td>
    <td>
        @Html.ActionLink("Rating", "Edit", new { id = item.Id }) |
        @Html.ActionLink("Delete", "Delete", new { id = item.Id })
    </td>
</tr>
}

</table>
